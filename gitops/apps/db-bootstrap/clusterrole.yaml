apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: db-bootstrap
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
rules:
- apiGroups: [ "" ]
  resources: [ "secrets" ]
  verbs: [ "get", "list", "create" ]
